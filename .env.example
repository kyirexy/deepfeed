# scripts/bootstrap.sh 会自动生成并写入随机值。
SEARXNG_SECRET=replace-with-a-random-secret
SEARXNG_SETTINGS_FILE=./infra/searxng/settings.local.yml

# 可选：采集器在 Docker 外运行时使用。
SEARXNG_URL=http://127.0.0.1:8080
RSSHUB_URL=http://127.0.0.1:1200
SEARCH_TIME_RANGE=month
MAX_QUERIES_PER_PLATFORM=2
